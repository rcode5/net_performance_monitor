import React from 'react';

const Header = (_props) => (
  <div className="header-container">
    <header className="wrapper clearfix">
      <h1 className="header__title">Network Speed Report</h1>
    </header>
  </div>);

export default Header;
